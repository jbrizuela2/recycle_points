<%= form_for category do |f| %>
  <div class="field">
    <%= f.label "Nombre", class: "label" %>
    <p class="control">
      <%= f.text_field :name, class: "input" %>
    </p>
  </div>

  <div class="field">
    <%= f.label "Descripción", class: "label" %>
    <p class="control">
      <%= f.text_area :description, class: "textarea", rows: 3 %>
    </p>
  </div>

  <div class="field">
    <%= f.label "Color", class: "label" %>
    <p class="control">
      <%= f.select :color, options_for_select(Category::COLORS.to_a) %>
    </p>
  </div>
  
  <%= f.submit "Guardar", class: "button is-success" %>
<% end %>
